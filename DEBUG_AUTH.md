# 除錯認證問題

## 🔍 問題：發送訊號時顯示 "Not authenticated"

這表示註冊後沒有正確設置 `authToken`。

## ✅ 解決步驟

### 1. 確認後端已更新

後端應該會自動重新載入（nodemon）。檢查後端終端機是否有錯誤。

### 2. 重新註冊測試

**重要：必須重新註冊**，因為舊的註冊可能沒有 session。

1. 在 App 中註冊一個**新帳號**（使用新的名字）
2. 查看 App 終端機，應該會看到：
   ```
   ✅ Registration successful, setting user: ...
   ✅ Auth token set
   ```
3. 如果**沒有看到** `✅ Auth token set`，表示註冊時沒有獲取到 session

### 3. 檢查後端日誌

在後端終端機中，註冊時應該會看到：
```
📝 Register request received: ...
✅ Auth user created: ...
⚠️ No session from signup, attempting to sign in...
✅ Session obtained from sign in
✅ User record created: ...
```

### 4. 測試發送訊號

註冊後，嘗試發送訊號，查看終端機：
- `🔐 createSignal called, authToken: ✅ ...` （應該看到 ✅）
- `📡 Attempting to send signal, authToken: ✅ Present` （應該看到 ✅）

---

## 🐛 如果還是 "Not authenticated"

可能的原因：
1. **Supabase 需要 email 確認** - 需要在 Dashboard 關閉 email 確認
2. **Session 獲取失敗** - 查看後端日誌是否有錯誤
3. **前端沒有正確接收** - 查看 App 終端機的註冊響應

---

## 📝 請告訴我：

重新註冊後，終端機中：
1. 有看到 `✅ Auth token set` 嗎？
2. 有看到 `✅ Session obtained from sign in` 嗎？
3. 發送訊號時，有看到 `authToken: ✅` 嗎？

根據這些訊息，我可以進一步診斷問題。






